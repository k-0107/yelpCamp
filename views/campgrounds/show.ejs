<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>キャンプ場詳細</title>
  </head>
  <body>
    <h1>キャンプ場詳細</h1>
    <p><%= campground.title %></p>
    <p><%= campground.location %></p>

    <a href="/campgrounds/<%= campground._id %>/edit">編集</a>
    <p>
        <form method="POST" action="/campgrounds/<%= campground._id %>?_method=DELETE"><button>削除する</button></form>
    </p>
    <a href="/campgrounds">一覧に戻る</a>
  </body>
</html>
